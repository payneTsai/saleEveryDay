<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.base.dao.CustomerDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.renren.modules.base.entity.CustomerEntity" id="customerMap">
        <result property="customerId" column="customer_id"/>
        <result property="customerName" column="customer_name"/>
        <result property="customerMobile" column="customer_mobile"/>
        <result property="gmtCreate" column="gmt_create"/>
        <result property="gmtModified" column="gmt_modified"/>
    </resultMap>

	<select id="queryObject" resultType="io.renren.modules.base.entity.CustomerEntity">
		select * from customer where customer_id = #{value}
	</select>

	<select id="queryList" resultType="io.renren.modules.base.entity.CustomerEntity">
		select * from customer
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by customer_id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from customer 
	</select>
	 
	<insert id="save" parameterType="io.renren.modules.base.entity.CustomerEntity" useGeneratedKeys="true" keyProperty="customerId">
		insert into customer
		(
			`customer_name`, 
			`customer_mobile`, 
			`gmt_create`, 
			`gmt_modified`
		)
		values
		(
			#{customerName}, 
			#{customerMobile}, 
			#{gmtCreate}, 
			#{gmtModified}
		)
	</insert>
	 
	<update id="update" parameterType="io.renren.modules.base.entity.CustomerEntity">
		update customer 
		<set>
			<if test="customerName != null">`customer_name` = #{customerName}, </if>
			<if test="customerMobile != null">`customer_mobile` = #{customerMobile}, </if>
			<if test="gmtCreate != null">`gmt_create` = #{gmtCreate}, </if>
			<if test="gmtModified != null">`gmt_modified` = #{gmtModified}</if>
		</set>
		where customer_id = #{customerId}
	</update>
	
	<delete id="delete">
		delete from customer where customer_id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from customer where customer_id in 
		<foreach item="customerId" collection="array" open="(" separator="," close=")">
			#{customerId}
		</foreach>
	</delete>

</mapper>